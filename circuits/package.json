{
  "name": "solmixer-circuits",
  "version": "1.0.0",
  "description": "ZK circuits for SolMixer",
  "scripts": {
    "compile": "circom mixer.circom --r1cs --wasm --sym",
    "setup": "snarkjs groth16 setup mixer.r1cs pot12_final.ptau mixer_0000.zkey",
    "export": "snarkjs zkey export verificationkey mixer_final.zkey verification_key.json"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.3"
  }
}
